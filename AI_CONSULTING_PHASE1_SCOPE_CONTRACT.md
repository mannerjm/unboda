# 운보다 AI 상담 Phase 1 — 상담 권한·범위 계약

## 목적

AI 상담은 반드시 유료 심층분석 구매 범위 안에서만 답변하며, 범위를 벗어난 질문에는 답변을 생성하지 않는다. 장기 기억은 상담 품질을 위한 컨텍스트이지 상담 권한을 넓히는 수단이 아니다.

이 문서는 구현 전에 상담 권한의 source of truth와 2중 제한 규칙을 고정한다. 이 단계에서는 Production DB, 결제, 사용자 데이터, UI를 변경하지 않는다.

## 1. 기존 코드에서 재사용할 source of truth

### 주제형 상품

`app/lib/paidAnalysisTopicConfig.ts`의 Launch topic config를 상담 권한의 1차 source of truth로 사용한다.

각 상품에 이미 다음 계약이 있다.

- `productId`
- `userQuestion`
- `analysisFocus`
- `requiredInsights`
- `excludedFocus`
- `prohibitedClaims`
- `decisionType`
- `actionFocus`

AI 상담용 별도 47개 수동 매핑을 새로 만들지 않는다. 수동 중복은 상품 수정 시 상담 범위가 어긋날 위험을 만든다.

상담 허용 범위는 `userQuestion + analysisFocus + requiredInsights`에서 파생한다.
상담 차단 범위는 `excludedFocus + prohibitedClaims`에서 파생한다.

### 기간형 상품

`app/lib/analysisPeriodStrategy.ts`를 상담 권한의 1차 source of truth로 사용한다.

Launch 기간형 상품은 다음 7개다.

1. `monthly-current`
2. `monthly-next`
3. `yearly-current`
4. `annual-next`
5. `annual-3years`
6. `daeun-current`
7. `lifetime-overview`

각 기간형 상품은 다음 계약을 사용한다.

- `timeGranularity`
- `coreQuestion`
- `focus`
- `requiredInsights`
- `timelineSpec`
- `prohibitedPatterns`

기간형은 특정 주제 하나에 한정하지 않지만, 구매한 기간 범위와 기존 분석 전략이 다루는 생활 영역 안에서만 답변한다.

## 2. 상담 권한 판정 순서

모든 사용자 질문은 생성형 답변 모델에 바로 전달하지 않는다.

서버는 반드시 아래 순서로 판정한다.

1. 로그인 사용자 확인
2. 현재 프로필 소유권 확인
3. 실제 구매/entitlement 확인
4. 구매 상품의 Launch 지원 여부 확인
5. 질문 길이와 형식 확인
6. 질문의 주제/기간/위험 범위 분류
7. 구매 상품 계약과 비교
8. `ALLOW | CLARIFY | DENY | SAFETY_REDIRECT` 중 하나로 판정
9. `ALLOW`일 때만 본 상담 답변 모델 호출
10. 정상 답변 확정 후에만 상담 횟수 차감

범위 밖 질문, 불명확 질문, 서버 오류는 상담 횟수를 차감하지 않는다.

## 3. 판정 결과 정의

### ALLOW

구매 상품의 허용 범위에 명확히 포함된다.

### CLARIFY

질문이 너무 모호해 범위 판정 자체가 불가능하다. AI가 추측해서 답하지 않고 짧은 추가 질문을 요청한다.

예: `앞으로 어떻게 될까요?`

### DENY

구매 범위를 벗어난다. 본 상담 생성 모델을 호출하지 않고 정적 안내문을 반환한다.

예: 이직 분석 구매 후 연애 결과 질문.

### SAFETY_REDIRECT

의료 진단, 법률 판단, 구체 투자 실행, 자해·위기 등 운보다 상담 범위를 넘어서는 고위험 요청이다. 사주 해석으로 대신 답하지 않고 적절한 제한/안내를 제공한다.

## 4. 주제형 제한 규칙

주제형 상담은 구매한 `productId`의 세부 계약을 기준으로 한다.

### 허용

- `userQuestion`의 직접 후속 질문
- `analysisFocus`의 구체화
- `requiredInsights`의 설명·적용
- 해당 리포트의 내용을 현실 상황에 연결하는 질문
- 기존 상담 기억 중 현재 상품 범위와 관련 있는 사실의 재참조

### 차단

- `excludedFocus`에 명시된 인접 상품 영역
- `prohibitedClaims`에 해당하는 단정/보장/예언
- 구매하지 않은 별도 상품의 핵심 질문
- 사용자가 과거에 다른 주제로 상담한 기록이 있다는 이유만으로 현재 권한을 확장하는 행위

## 5. 기간형 제한 규칙

기간형은 `시간 범위 + 분석 전략 범위 + 안전 범위`를 동시에 만족해야 한다.

### monthly-current

현재 월 내부 흐름만 허용한다. 다른 월/연도에 대한 독립 예측은 차단한다.

### monthly-next

다음 달 진입 준비와 다음 달 내부 관찰 범위만 허용한다.

### yearly-current

현재 연도 내부의 연간 운영·우선순위·검토 질문을 허용한다.

### annual-next

다음 해의 준비·구간·운영 질문을 허용한다.

### annual-3years

해당 3개년 범위 내 비교·전환·우선순위 질문을 허용한다. 범위 밖 특정 연도는 차단한다.

### daeun-current

현재 대운 구간의 장기 흐름과 현재 구간에서의 운영 질문만 허용한다. 특정 사건의 확정 시점 예언은 차단한다.

### lifetime-overview

생애 전반의 장기 패턴·전환·우선순위 탐색을 허용하되, 특정 날짜/사건/성과를 확정하는 예언으로 확장하지 않는다.

## 6. 기억과 권한은 분리한다

장기 기억 데이터는 다음 목적으로만 사용한다.

- 사용자가 직접 말한 실제 상황 기억
- 이전 상담에서 합의한 목표/선택지/후속 확인사항 기억
- 구매한 분석과 관련된 과거 상담 요약 제공
- 반복 설명 비용 감소

기억은 절대로 다음을 허용하지 않는다.

- 이미 만료/소진된 상담권을 되살리는 것
- 구매하지 않은 상품 범위로 답변 범위를 확장하는 것
- 과거 AI 추론을 사용자 사실로 승격하는 것

기억 항목은 반드시 출처 유형을 가진다.

- `USER_STATED_FACT`
- `PROFILE_FACT`
- `PURCHASED_REPORT_FACT`
- `CONSULTATION_SUMMARY`
- `AI_INFERENCE`

`AI_INFERENCE`는 다음 상담에서 사실처럼 사용하지 않는다.

## 7. 비용 통제 원칙

- 질문 입력 상한은 초기 300자로 설계한다.
- 기본 답변은 약 500~800자 범위를 목표로 한다.
- 범위 판정 단계는 가능한 한 저비용 구조로 처리한다.
- 범위 밖 질문에는 본 상담 생성 모델을 호출하지 않는다.
- 과거 전체 대화를 매번 프롬프트에 넣지 않는다.
- `장기 기억 요약 + 관련 과거 상담 + 최근 소수 메시지 + 현재 구매 리포트의 필요한 부분`만 전달한다.
- 출력 토큰 상한을 서버에서 강제한다.

## 8. 2중 방어

상담 제한은 프롬프트에만 의존하지 않는다.

### 1차 방어 — 서버 권한 엔진

구매권한, 상품 범위, 기간 범위, 위험 요청을 서버가 판정한다.

### 2차 방어 — 답변 프롬프트

본 상담 모델에도 현재 상품 범위, 금지 주장, 사실/해석 구분 규칙을 다시 전달한다.

둘 중 하나라도 허용 조건을 만족하지 못하면 답변을 생성하지 않는다.

## 9. 사실성 표현 규칙

답변은 항상 다음 세 층을 구분한다.

1. 사용자가 직접 제공한 실제 사실
2. 운보다의 저장된 명리 분석 내용
3. AI가 현재 질문에 대해 정리한 해석/선택지

AI는 명리 해석을 객관적 사실이나 확정적 미래로 표현하지 않는다.

금지 예:

- `2027년 3월에 반드시 이직합니다.`
- `상대방은 당신을 아직 사랑합니다.`
- `이 종목을 사면 돈을 벌 수 있습니다.`

허용 방향 예:

- `저장된 분석에서는 변화 압력이 커지는 흐름으로 해석되어 있습니다.`
- `당신이 말한 현재 상황과 함께 보면, 확인할 선택지는 A/B로 정리할 수 있습니다.`

## 10. 다음 구현 단계

Phase 2에서는 Production 동작을 아직 열지 않고 다음 코드만 만든다.

1. `ConsultationScopeDecision` 타입
2. 상품 계약을 읽는 server-only scope resolver
3. `ALLOW | CLARIFY | DENY | SAFETY_REDIRECT` 결정 함수
4. 47개 주제형 Launch 상품과 7개 기간형 Launch 상품의 coverage regression
5. 범위 밖 질문이 생성 모델까지 도달하지 않는 regression

Phase 2가 통과하기 전에는 채팅 UI, 결제 차감, Production DB migration을 추가하지 않는다.
